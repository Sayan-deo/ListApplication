stages:
 - build

default:
 image: node:20-alpine

cache:
 key:
  files:
   - package-lock.json
 paths:
  - .npm/

build:
 stage: build
 script:
  - npm ci --cache .npm --prefer-offline
 artifacts:
  name: "node-dependencies-$CI_COMMIT_SHORT_SHA"
  paths: 
   - node_modules/
  expires_in: 1 hour